# TF-Gen
TF-Gen (Text File Generators) contains two python scripts. One of which generates username lists and the other generates password lists that can be paired with tools like Hydra for brute force attacks.
